{
  "name": "enough-home",
  "version": "1.0.0",
  "description": "Cloudflare Worker for ENOUGHGAMBLING website",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
